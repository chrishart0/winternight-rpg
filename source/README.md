# Source boundary

Private notes belong in `source/private/` and must remain untracked. The compiler consumes these files together with `design/` specifications to regenerate the complete four-chapter project.
