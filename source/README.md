# Source boundary

Private notes belong in `source/private/` and must remain untracked. Checked-in source data contains only provenance-safe structured facts, character/location definitions, adaptation decisions, and beat IDs—never novel text or substantial excerpts. The compiler consumes these files together with `design/` specifications to regenerate the complete four-chapter project.
