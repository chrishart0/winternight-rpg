# Asset contract

An approved asset needs stable identity/location IDs, source/output hashes, generation and processing provenance, exact engine dimensions/format, approval state, and a license note. Portrait variants preserve identity anchors. Build registration consumes only approved processed outputs.

Phase 0 placeholders are deterministic geometric assets marked `programmatic_placeholder`; they are not AI-generated.
