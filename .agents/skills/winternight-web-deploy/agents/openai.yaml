interface:
  display_name: "Winternight Web Deploy"
  short_description: "Build and publish the verified S3 browser POC"
  default_prompt: "Use $winternight-web-deploy to build, publish, and verify the current Winternight browser build on production S3."
policy:
  allow_implicit_invocation: false
